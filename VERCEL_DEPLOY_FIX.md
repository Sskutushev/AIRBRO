# 🚨 КРИТИЧЕСКАЯ ОШИБКА: Vercel деплой требует секретов

## 🔍 Проблема:

Vercel деплой падает с ошибкой: `Error: Input required and not supplied: vercel-token`

## 🛠 ИСПРАВЛЕНИЕ:

### Вариант 1: Настроить секреты в GitHub (рекомендуется)

В GitHub репозитории добавьте следующие секреты:

1. **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret** для каждого:

```bash
# Vercel Configuration
VERCEL_TOKEN=your_vercel_token_here
VERCEL_ORG_ID=your_vercel_org_id
VERCEL_PROJECT_ID=your_vercel_project_id

# Environment Variables
VITE_API_URL=https://airbro-production.up.railway.app
VITE_SENTRY_DSN=https://c908da1d6af98b5e706a07842906793d@o4510323850149888.ingest.de.sentry.io/4510323853754448
VITE_GA_MEASUREMENT_ID=G_-88zE_Qqel0mha3AjFMg

# Sentry (опционально)
SENTRY_AUTH_TOKEN=your_sentry_auth_token
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project
```

### Вариант 2: Ручной деплой (временно)

Пока секреты не настроены, деплойте вручную:

```bash
# Локально
npm run build
npx vercel --prod
```

### Вариант 3: Использовать Vercel GitHub интеграцию

1. Подключите репозиторий к Vercel через веб-интерфейс
2. Vercel автоматически настроит деплой
3. Секреты будут настроены автоматически

## 🔧 Как получить Vercel секреты:

### VERCEL_TOKEN:

1. Перейдите на https://vercel.com/account/tokens
2. Создайте новый токен
3. Скопируйте токен

### VERCEL_ORG_ID и VERCEL_PROJECT_ID:

1. Перейдите в проект Vercel
2. Settings → General
3. Скопируйте Organization ID и Project ID

## ✅ Что исправлено:

1. **Отключен автоматический Vercel деплой** до настройки секретов
2. **Исправлена переменная API URL** (убрал \_PROD суффикс)
3. **Добавлены инструкции** по настройке секретов
4. **Сборка проекта** теперь работает без ошибок

## 🎯 Следующие шаги:

1. **Настройте секреты** в GitHub (Вариант 1)
2. **Или подключите репозиторий** к Vercel через веб-интерфейс (Вариант 3)
3. **После настройки** раскоментируйте секции деплоя в workflow файле

## 📝 Временное решение:

Пока секреты не настроены, используйте ручной деплой:

```bash
npm run build
npx vercel --prod
```
