# 🚀 Инструкции по завершению настройки деплоя

## ✅ Что уже исправлено:

1. **API URL**: Обновлен на production backend `https://api.aibrobusiness.com`
2. **CORS**: Добавлен Vercel домен в разрешенные источники
3. **vercel.json**: Убрал проксирование на localhost
4. **Environment**: Создан файл `.env.production` с правильными переменными

## 🔧 Что нужно сделать:

### 1. Настройка переменных окружения в Vercel

В панели управления Vercel добавьте следующие переменные окружения:

```
VITE_API_URL=https://api.aibrobusiness.com
VITE_SENTRY_DSN=https://c908da1d6af98b5e706a07842906793d@o4510323850149888.ingest.de.sentry.io/4510323853754448
VITE_GA_MEASUREMENT_ID=G_-88zE_Qqel0mha3AjFMg
```

### 2. Деплой бэкенда на Railway

Убедитесь, что бэкенд развернут на Railway и доступен по адресу `https://api.aibrobusiness.com`.

### 3. Обновление CORS в бэкенде

В файле `backend/src/server.ts` убедитесь, что CORS настроен правильно:

```typescript
const corsOptions = {
  origin: [
    process.env.FRONTEND_URL || 'http://localhost:5173',
    'https://airbro-mrqs.vercel.app',
    'http://localhost:5173',
  ],
  credentials: true,
};
```

## 🧪 Проверка работы:

1. После деплоя фронтенда на Vercel, перейдите на сайт
2. Попробуйте зарегистрироваться или войти в систему
3. Проверьте консоль браузера на наличие ошибок CORS

## 🔍 Если ошибки остались:

1. **Проверьте health check**: `https://api.aibrobusiness.com/health`
2. **Проверьте CORS headers** в ответе сервера
3. **Убедитесь, что бэкенд развернут** и доступен

## 📝 Измененные файлы:

- `vercel.json` - убрал проксирование на localhost
- `backend/src/server.ts` - добавил Vercel домен в CORS
- `.env` - обновил API URL на production
- `.env.production` - создал файл с production переменными
- `.env.example` - обновил примеры конфигурации

Все изменения уже отправлены на GitHub и будут автоматически развернуты.
