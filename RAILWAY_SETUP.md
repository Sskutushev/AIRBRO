# 🚀 Инструкции по настройке Railway для AIRBRO Backend

## ✅ Что уже готово:

1. **Railway токен добавлен** в GitHub Secrets: `RAILWAY_TOKEN`
2. **Workflow настроен** для автоматического деплоя
3. **Health check** настроен для проверки работы API

## 🛠 Что нужно сделать в Railway Dashboard:

### 1. Создать проект на Railway:

1. Перейдите на https://railway.app
2. Войдите в аккаунт
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Найдите и выберите репозиторий AIRBRO

### 2. Настроить переменные окружения в Railway:

В панели Railway проекта добавьте следующие переменные:

```env
# Server Configuration
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://airbro-mrqs.vercel.app

# Database
DATABASE_URL=file:./prisma/dev.db

# Security
JWT_SECRET=d5f014ab7534ff7fe23c4f9d761bc640
JWT_EXPIRES_IN=7d

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=7689714723:AAGnAFlL_skB5_HSaH-TXshS38fnGg6QSW0
TELEGRAM_ADMIN_CHANNEL=-1003293972627

# Crypto Wallets (опционально)
USDT_TRC20_WALLET=TMeZwVH4URxUbyXNcbChdakH4ZHML1Cj5g
USDT_ERC20_WALLET=0xc31b59dCb04ae80ae4c29b7FD5063f127c56a3A8
TON_WALLET=UQA_EtrTTvpsRJDtB69IfycEaocXgdwC0hNG4R_h_2kcd4sG

# Currency Rates (опционально)
RUB_TO_USDT_RATE=95.5
RUB_TO_TON_RATE=15.2
```

### 3. Настроить подключение к базе данных:

1. В Railway проекте нажмите "New" → "Database" → "Add PostgreSQL"
2. Скопируйте URL базы данных
3. Обновите переменную `DATABASE_URL` в Railway с URL PostgreSQL

### 4. Проверить деплой:

После настройки:

1. Railway автоматически начнет деплой
2. Проверьте логи деплоя в Railway Dashboard
3. Убедитесь, что API доступен по адресу: `https://airbro-production.up.railway.app/health`

## 🔄 Автоматический деплой:

После настройки Railway проекта:

1. Любые изменения в папке `backend/` будут автоматически деплоиться
2. Деплой запускается при `push` в ветку `main`
3. Health check проверит работу API после деплоя

## 🧪 Тестирование:

После успешного деплоя:

1. Проверьте: https://airbro-production.up.railway.app/health
2. Должен вернуть: `{"status":"OK","timestamp":"..."}`
3. Проверьте авторизацию на сайте: https://airbro-mrqs.vercel.app

## ⚠️ Возможные проблемы:

1. **Ошибка миграций**: Убедитесь, что PostgreSQL настроен правильно
2. **CORS ошибки**: Проверьте, что `FRONTEND_URL` настроен правильно
3. **JWT ошибки**: Убедитесь, что `JWT_SECRET` установлен

## 📞 Поддержка:

Если возникнут проблемы:

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения настроены
3. Проверьте статус деплоя в GitHub Actions
