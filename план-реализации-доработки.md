# ПРОМПТ ДЛЯ РЕАЛИЗАЦИИ BACKEND AIBRO BUSINESS

## 🎯 КОНТЕКСТ ПРОЕКТА

AIBRO Business - это экосистема AI-инструментов для автоматизации малого и среднего бизнеса, полностью интегрированная в Telegram. Frontend полностью готов (React + TypeScript + Tailwind + i18n). Нужно реализовать backend для полноценной работы системы с платежами, корзиной и личным кабинетом.

## 📋 ТЕКУЩЕЕ СОСТОЯНИЕ

### ✅ Что готово:
- Frontend: 12 секций, дизайн-система, анимации, локализация (RU/EN)
- UI Components: Button, Card, Modal, Form elements
- Contexts: AuthContext, SubscriptionContext, ThemeContext
- Pages: HomePage, AccountPage, PaymentPage, AuthPage
- Форма обратной связи с Telegram интеграцией

### ❌ Что нужно реализовать:
- **Backend API:** Полностью отсутствует
- **База данных:** Не настроена
- **Система аутентификации:** Реальная JWT-авторизация
- **Платежная система:** Крипто-платежи (USDT/TON)
- **Корзина и чекаут:** Полный flow покупки
- **Личный кабинет:** История покупок, управление подписками

---

## 🎯 ГЛАВНАЯ ЗАДАЧА

Создай **МАКСИМАЛЬНО ПРОСТОЙ** и **БЕСПЛАТНЫЙ В РАЗВЕРТЫВАНИИ** backend для AIBRO Business со следующими требованиями:

### Критические требования:
1. **Простота развертывания:** Должен запускаться локально одной командой
2. **Бесплатность:** SQLite локально, легкая миграция на PostgreSQL для продакшена
3. **Минимум зависимостей:** Только необходимые пакеты
4. **Типизация:** TypeScript везде
5. **Безопасность:** JWT авторизация, валидация, защита от основных атак

---

## 🏗️ ТЕХНИЧЕСКАЯ АРХИТЕКТУРА

### Стек технологий:
```yaml
Backend Framework: Express.js + TypeScript
Database: SQLite (dev) → PostgreSQL (prod)
ORM: Prisma
Authentication: JWT + bcrypt
Validation: Zod
Payment: Крипто (USDT TRC20/ERC20, TON)
Telegram: node-telegram-bot-api
Environment: dotenv
```

### Структура проекта:
```
backend/
├── src/
│   ├── config/           # Конфигурация (DB, JWT, Telegram)
│   ├── middleware/       # Auth, validation, error handling
│   ├── routes/           # API endpoints
│   ├── controllers/      # Бизнес-логика
│   ├── services/         # Сервисный слой
│   ├── models/           # TypeScript типы
│   ├── utils/            # Утилиты
│   ├── prisma/           # Prisma schema и migrations
│   └── server.ts         # Entry point
├── .env.example
├── package.json
├── tsconfig.json
└── README.md
```

---

## 📊 СХЕМА БАЗЫ ДАННЫХ

### Prisma Schema (детально):

```prisma
// Пользователь
model User {
  id            String         @id @default(uuid())
  email         String         @unique
  passwordHash  String
  name          String
  telegram      String         @unique
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  
  subscriptions Subscription[]
  payments      Payment[]
  cartItems     CartItem[]
  
  @@map("users")
}

// Продукт
model Product {
  id          String   @id @default(uuid())
  slug        String   @unique
  name        String
  description String
  price       Int      // В копейках
  interval    String   // "month" или "year"
  features    String   // JSON с фичами
  isActive    Boolean  @default(true)
  tier        Int      // 1, 2, 3
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  subscriptions Subscription[]
  cartItems     CartItem[]
  
  @@map("products")
}

// Корзина
model CartItem {
  id        String   @id @default(uuid())
  userId    String
  productId String
  quantity  Int      @default(1)
  createdAt DateTime @default(now())
  
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  product Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  
  @@unique([userId, productId])
  @@map("cart_items")
}

// Подписка
model Subscription {
  id              String    @id @default(uuid())
  userId          String
  productId       String
  status          String    // "active", "cancelled", "expired", "trial"
  startDate       DateTime
  endDate         DateTime
  nextPaymentDate DateTime?
  cancelledAt     DateTime?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  product Product @relation(fields: [productId], references: [id])
  
  @@map("subscriptions")
}

// Платеж
model Payment {
  id            String    @id @default(uuid())
  userId        String
  amount        Int       // В копейках
  currency      String    // "RUB", "USD", "USDT", "TON"
  status        String    // "pending", "completed", "failed", "expired"
  paymentMethod String    // "crypto_usdt_trc20", "crypto_usdt_erc20", "crypto_ton"
  
  // Крипто данные
  walletAddress String?
  txHash        String?
  qrCode        String?   // Base64 QR-код
  
  // Метаданные
  metadata      String?   // JSON
  expiresAt     DateTime?
  
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@map("payments")
}
```

---

## 🔐 СИСТЕМА АУТЕНТИФИКАЦИИ

### Требования:
1. **Регистрация:** Email + Пароль + Имя + Telegram username
2. **Авторизация:** Email + Пароль → JWT токен
3. **Защита роутов:** Middleware для проверки JWT
4. **Безопасность:** bcrypt для паролей (10 раундов)

### API Endpoints:

#### POST /api/auth/register
```typescript
Request Body:
{
  email: string;      // Валидация: email формат
  password: string;   // Минимум 8 символов
  name: string;       // Минимум 2 символа
  telegram: string;   // Формат: @username
}

Response (201):
{
  user: {
    id: string;
    email: string;
    name: string;
    telegram: string;
    createdAt: string;
  };
  token: string; // JWT токен
}

Errors:
400 - Validation error
409 - User already exists
```

#### POST /api/auth/login
```typescript
Request Body:
{
  email: string;
  password: string;
}

Response (200):
{
  user: {
    id: string;
    email: string;
    name: string;
    telegram: string;
  };
  token: string;
}

Errors:
400 - Validation error
401 - Invalid credentials
```

#### GET /api/auth/me
```typescript
Headers:
Authorization: Bearer {token}

Response (200):
{
  id: string;
  email: string;
  name: string;
  telegram: string;
  createdAt: string;
}

Errors:
401 - Unauthorized
```

---

## 🛒 КОРЗИНА И ЧЕКАУТ

### Требования:
1. **Корзина:** Добавление/удаление продуктов
2. **Подсчет суммы:** Автоматический расчет
3. **Валидация:** Проверка наличия продуктов
4. **Очистка:** После успешной оплаты

### API Endpoints:

#### GET /api/cart
```typescript
Headers:
Authorization: Bearer {token}

Response (200):
{
  items: [
    {
      id: string;
      product: {
        id: string;
        name: string;
        price: number;
        description: string;
      };
      quantity: number;
    }
  ];
  total: number; // Сумма в копейках
}
```

#### POST /api/cart/add
```typescript
Request Body:
{
  productId: string;
  quantity: number; // Default: 1
}

Response (200):
{
  cartItem: {
    id: string;
    productId: string;
    quantity: number;
  }
}

Errors:
404 - Product not found
409 - Already in cart
```

#### DELETE /api/cart/:itemId
```typescript
Response (200):
{
  message: "Товар удален из корзины"
}

Errors:
404 - Item not found
```

#### POST /api/cart/clear
```typescript
Response (200):
{
  message: "Корзина очищена"
}
```

---

## 💳 ПЛАТЕЖНАЯ СИСТЕМА (КРИПТО)

### Требования:
1. **Поддержка:** USDT (TRC20, ERC20), TON
2. **Генерация QR:** Автоматическое создание QR-кода
3. **Таймер:** 30 минут на оплату
4. **Конвертация:** RUB → USDT/TON по курсу
5. **Предупреждения:** О рисках и правильности адреса

### Конфигурация кошельков (.env):
```bash
# Крипто кошельки (заменить на реальные!)
USDT_TRC20_WALLET=TYourWalletAddressHere
USDT_ERC20_WALLET=0xYourWalletAddressHere
TON_WALLET=EQYourTonWalletAddressHere

# Курсы (или API для получения)
RUB_TO_USDT_RATE=0.011
RUB_TO_TON_RATE=0.17
```

### API Endpoints:

#### POST /api/payments/crypto/create
```typescript
Request Body:
{
  cartItems: string[]; // IDs товаров
  paymentMethod: "crypto_usdt_trc20" | "crypto_usdt_erc20" | "crypto_ton";
}

Response (200):
{
  paymentId: string;
  walletAddress: string;
  amountRub: number;
  amountCrypto: number;
  currency: "USDT" | "TON";
  qrCode: string; // Base64 data URL
  expiresAt: string; // ISO 8601
  network: "TRC20" | "ERC20" | "TON";
  warnings: string[]; // Массив предупреждений
}

Warnings example:
[
  "⚠️ ВНИМАНИЕ: Криптовалютные платежи несут риски!",
  "✓ Отправляйте ТОЛЬКО USDT в сети TRC20",
  "✓ Тщательно проверьте адрес кошелька",
  "✓ Отправка на неправильный адрес приведет к потере средств",
  "✓ Платеж действителен 30 минут"
]
```

#### GET /api/payments/:id/status
```typescript
Response (200):
{
  status: "pending" | "completed" | "failed" | "expired";
  txHash: string | null;
  expiresAt: string;
  timeLeft: number; // Секунды до истечения
}
```

#### POST /api/payments/:id/confirm (Admin only)
```typescript
Request Body:
{
  txHash: string; // Хеш транзакции
}

Response (200):
{
  payment: {
    id: string;
    status: "completed";
    txHash: string;
  };
  subscriptions: [...]; // Созданные подписки
}
```

### Заглушка для банковских карт:

#### POST /api/payments/card/create
```typescript
Response (501):
{
  error: "В разработке",
  message: "Оплата банковскими картами будет доступна в ближайшее время",
  availableMethods: ["crypto_usdt_trc20", "crypto_usdt_erc20", "crypto_ton"]
}
```

---

## 👤 ЛИЧНЫЙ КАБИНЕТ

### Требования:
1. **Профиль:** Просмотр и редактирование данных
2. **Подписки:** Список активных подписок
3. **История платежей:** Все транзакции
4. **Управление:** Отмена подписок

### API Endpoints:

#### GET /api/user/profile
```typescript
Response (200):
{
  id: string;
  email: string;
  name: string;
  telegram: string;
  createdAt: string;
}
```

#### PUT /api/user/profile
```typescript
Request Body:
{
  name?: string;
  telegram?: string;
}

Response (200):
{
  user: {...}
}
```

#### GET /api/user/subscriptions
```typescript
Response (200):
{
  subscriptions: [
    {
      id: string;
      product: {
        name: string;
        description: string;
      };
      status: string;
      startDate: string;
      endDate: string;
      nextPaymentDate: string | null;
    }
  ]
}
```

#### GET /api/user/payments
```typescript
Response (200):
{
  payments: [
    {
      id: string;
      amount: number;
      currency: string;
      status: string;
      paymentMethod: string;
      createdAt: string;
    }
  ]
}
```

#### POST /api/user/subscriptions/:id/cancel
```typescript
Response (200):
{
  subscription: {
    id: string;
    status: "cancelled";
    cancelledAt: string;
  }
}
```

---

## 📦 УПРАВЛЕНИЕ ПРОДУКТАМИ

### Seed данных (Prisma seed):

```typescript
// prisma/seed.ts
const products = [
  {
    slug: "ai-postmaster-starter",
    name: "AI PostMaster Starter",
    description: "1 канал, 30 постов/месяц",
    price: 300000, // 3000 рублей в копейках
    interval: "month",
    tier: 1,
    features: JSON.stringify([
      "1 Telegram канал",
      "30 постов в месяц",
      "Базовая персонализация",
      "Email поддержка"
    ])
  },
  {
    slug: "ai-postmaster-pro",
    name: "AI PostMaster Pro",
    description: "1 канал, неограниченные посты",
    price: 500000,
    interval: "month",
    tier: 1,
    features: JSON.stringify([
      "1 Telegram канал",
      "Неограниченные посты",
      "Продвинутая персонализация",
      "Аналитика",
      "Приоритетная поддержка"
    ])
  },
  // ... остальные продукты
];
```

### API Endpoints:

#### GET /api/products
```typescript
Query params:
- tier?: number (фильтр по tier)
- isActive?: boolean (только активные)

Response (200):
{
  products: [
    {
      id: string;
      slug: string;
      name: string;
      description: string;
      price: number;
      interval: string;
      features: string[]; // Parsed JSON
      tier: number;
    }
  ]
}
```

#### GET /api/products/:slug
```typescript
Response (200):
{
  id: string;
  slug: string;
  name: string;
  description: string;
  price: number;
  interval: string;
  features: string[];
  tier: number;
}

Errors:
404 - Product not found
```

---

## 🔔 TELEGRAM ИНТЕГРАЦИЯ

### Требования:
1. **Уведомления:** О новых платежах, подписках
2. **Бот:** Приветственные сообщения
3. **Логирование:** Отправка в канал администратора

### Конфигурация (.env):
```bash
TELEGRAM_BOT_TOKEN=7689714723:AAGnAFlL_skB5_HSaH-TXshS38fnGg6QSW0
TELEGRAM_ADMIN_CHANNEL=-1002293972627
TELEGRAM_SUPPORT_BOT=@AIBROSupportBot
```

### События для отправки:

1. **Новая регистрация:**
```
🆕 Новый пользователь

👤 Имя: {name}
📧 Email: {email}
💬 Telegram: {telegram}
📅 Дата: {createdAt}
```

2. **Новый платеж:**
```
💰 Новый платеж

💵 Сумма: {amount} {currency}
🔐 Метод: {paymentMethod}
👤 Пользователь: {userName} (@{telegram})
📊 Статус: {status}
🔗 ID: {paymentId}
```

3. **Подтверждение оплаты:**
```
✅ Платеж подтвержден

💵 Сумма: {amount} {currency}
🔗 TX Hash: {txHash}
👤 Пользователь: {userName}
🎁 Продукты: {productNames}
```

---

## 🛡️ БЕЗОПАСНОСТЬ

### Middleware:

#### 1. Authentication Middleware
```typescript
// Проверка JWT токена
// Добавление userId в req.userId
// Обработка истекших токенов
```

#### 2. Validation Middleware
```typescript
// Использование Zod схем
// Валидация body, query, params
// Возврат понятных ошибок
```

#### 3. Error Handler Middleware
```typescript
// Централизованная обработка ошибок
// Логирование
// Безопасные сообщения клиенту (не раскрывать внутренние детали)
```

#### 4. Rate Limiting
```typescript
// Ограничение по IP
// Разные лимиты для разных endpoints
// 100 запросов/15 минут для auth
// 1000 запросов/15 минут для остального
```

### Валидация (Zod схемы):

```typescript
// Регистрация
const registerSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  name: z.string().min(2),
  telegram: z.string().regex(/^@[a-zA-Z0-9_]{5,}$/)
});

// Добавление в корзину
const addToCartSchema = z.object({
  productId: z.string().uuid(),
  quantity: z.number().int().min(1).default(1)
});

// Создание платежа
const createPaymentSchema = z.object({
  cartItems: z.array(z.string().uuid()),
  paymentMethod: z.enum([
    "crypto_usdt_trc20",
    "crypto_usdt_erc20",
    "crypto_ton"
  ])
});
```

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. Логирование
```typescript
// Winston или Pino
// Уровни: error, warn, info, debug
// Ротация файлов
// Отправка критических ошибок в Telegram
```

### 2. CORS
```typescript
const corsOptions = {
  origin: process.env.FRONTEND_URL || 'http://localhost:5173',
  credentials: true
};
```

### 3. Environment Variables
```bash
# Server
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# Database
DATABASE_URL="file:./dev.db" # SQLite для разработки
# DATABASE_URL="postgresql://user:pass@localhost:5432/aibro" # PostgreSQL для продакшена

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Crypto Wallets
USDT_TRC20_WALLET=TYourWalletAddressHere
USDT_ERC20_WALLET=0xYourWalletAddressHere
TON_WALLET=EQYourTonWalletAddressHere

# Exchange Rates
RUB_TO_USDT_RATE=0.011
RUB_TO_TON_RATE=0.17

# Telegram
TELEGRAM_BOT_TOKEN=7689714723:AAGnAFlL_skB5_HSaH-TXshS38fnGg6QSW0
TELEGRAM_ADMIN_CHANNEL=-1002293972627
TELEGRAM_SUPPORT_BOT=@AIBROSupportBot
```

### 4. Scripts (package.json)
```json
{
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:seed": "tsx prisma/seed.ts",
    "test": "jest"
  }
}
```

---

## 🚀 ЗАПУСК И РАЗВЕРТЫВАНИЕ

### Локальная разработка:
```bash
# 1. Установка зависимостей
cd backend
npm install

# 2. Настройка .env
cp .env.example .env
# Отредактировать .env

# 3. Инициализация БД
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed

# 4. Запуск
npm run dev
```

### Production (Railway/Render):
```bash
# 1. Изменить DATABASE_URL на PostgreSQL
# 2. Установить переменные окружения
# 3. Deploy через Git push
```

---

## ✅ ЧЕКЛИСТ РЕАЛИЗАЦИИ

### Фаза 1: Базовая инфраструктура
- [ ] Настроить проект (package.json, tsconfig.json)
- [ ] Создать Prisma schema
- [ ] Настроить Express сервер
- [ ] Создать базовые middleware (auth, validation, errors)
- [ ] Написать seed данные

### Фаза 2: Аутентификация
- [ ] Реализовать регистрацию
- [ ] Реализовать авторизацию
- [ ] Создать auth middleware
- [ ] Endpoint /api/auth/me

### Фаза 3: Продукты
- [ ] Endpoint GET /api/products
- [ ] Endpoint GET /api/products/:slug
- [ ] Seed продуктов

### Фаза 4: Корзина
- [ ] GET /api/cart
- [ ] POST /api/cart/add
- [ ] DELETE /api/cart/:itemId
- [ ] POST /api/cart/clear

### Фаза 5: Платежи
- [ ] Crypto payment service (QR generation)
- [ ] POST /api/payments/crypto/create
- [ ] GET /api/payments/:id/status
- [ ] POST /api/payments/:id/confirm
- [ ] Заглушка для card payments

### Фаза 6: Личный кабинет
- [ ] GET /api/user/profile
- [ ] PUT /api/user/profile
- [ ] GET /api/user/subscriptions
- [ ] GET /api/user/payments
- [ ] POST /api/user/subscriptions/:id/cancel

### Фаза 7: Telegram интеграция
- [ ] Функция отправки в Telegram
- [ ] Уведомления о регистрации
- [ ] Уведомления о платежах
- [ ] Логирование в канал

### Фаза 8: Тестирование и деплой
- [ ] Тестирование всех endpoints
- [ ] Проверка безопасности
- [ ] Документация API
- [ ] Деплой на Railway/Render

---

## 📚 ПРИМЕР ИСПОЛЬЗОВАНИЯ

### Полный flow покупки:

```typescript
// 1. Регистрация
POST /api/auth/register
Body: { email, password, name, telegram }
→ Получаем token

// 2. Просмотр продуктов
GET /api/products
→ Выбираем продукт

// 3. Добавление в корзину
POST /api/cart/add
Headers: { Authorization: Bearer {token} }
Body: { productId, quantity: 1 }

// 4. Просмотр корзины
GET /api/cart
Headers: { Authorization: Bearer {token} }
→ Видим total

// 5. Создание платежа
POST /api/payments/crypto/create
Headers: { Authorization: Bearer {token} }
Body: { cartItems: [...], paymentMethod: "crypto_usdt_trc20" }
→ Получаем walletAddress, qrCode, warnings

// 6. Проверка статуса (периодически)
GET /api/payments/{paymentId}/status
→ status: "pending" | "completed" | "expired"

// 7. После оплаты - просмотр подписок
GET /api/user/subscriptions
Headers: { Authorization: Bearer {token} }
→ Видим активные подписки
```

---

## 🎯 КРИТЕРИИ УСПЕХА

1. ✅ **Простота:** Запускается одной командой `npm run dev`
2. ✅ **Безопасность:** JWT, валидация, rate limiting
3. ✅ **Функциональность:** Весь flow от регистрации до покупки работает
4. ✅ **Telegram:** Уведомления отправляются
5. ✅ **Документация:** README с инструкциями
6. ✅ **Типизация:** TypeScript без any
7. ✅ **Ошибки:** Понятные сообщения об ошибках
8. ✅ **Production-ready:** Легко деплоится на Railway/Render

---

## 💡 ВАЖНЫЕ ЗАМЕТКИ

### О крипто-платежах:
⚠️ **ЗАГЛУШКА:** Реальная проверка транзакций в блокчейне требует:
- API Tron/Ethereum (TronGrid, Infura)
- Или webhook от платежного провайдера (CoinPayments, NOWPayments)
- Сейчас: Ручное подтверждение через POST /api/payments/:id/confirm

### О платежных системах:
- **Банковские карты:** Интеграция требует юр.лица и договоров (ЮKassa, Stripe)
- **Сейчас:** Заглушка с ответом 501 "В разработке"
- **Будущее:** Когда будет готово юр.лицо

### О масштабировании:
- **SQLite → PostgreSQL:** Одна строка в .env
- **Горизонтальное масштабирование:** Stateless API, JWT в headers
- **Кэширование:** Redis для сессий (опционально)

---

## 📖 ИТОГОВЫЙ РЕЗУЛЬТАТ

После реализации получим:
1. **Полностью рабочий backend** с авторизацией
2. **Систему покупок** с корзиной и крипто-платежами
3. **Личный кабинет** с историей и управлением
4. **Telegram интеграцию** для уведомлений
5. **Production-ready** код, готовый к деплою
6. **Документацию** для запуска и использования

---

**Версия:** v1.0.0
**Дата:** Ноябрь 2025
**Автор промпта:** Claude 3.5 Sonnet

---

## 🎬 НАЧИНАЕМ РЕАЛИЗАЦИЮ

Используй этот промпт как подробное техническое задание. Реализуй каждую фазу последовательно, тестируя каждый endpoint перед переходом к следующему.

**Удачи! 🚀**