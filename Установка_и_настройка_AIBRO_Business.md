# Установка и настройка AIBRO Business

## 1. НАСТРОЙКА КЛЮЧЕЙ API (обязательно)

### Telegram Bot:
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например, "AIBRO Business Bot")
4. Придумайте юзернейм (например, "aibro_business_bot")
5. Скопируйте токен, который вы получите (вида: `7689714723:AAGnAFlL_skB5_HSaH-TXshS38fnGg6QSW0`)
6. В файле `C:\Users\sskut\Desktop\AIRBRO\backend\.env` замените:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_здесь
   ```

### Telegram Канал/Чат:
1. Создайте приватный чат или канал в Telegram
2. Добавьте созданного бота администратором
3. Найдите его ID (через @userinfobot или другие способы)
4. В файле `.env` замените:
   ```
   TELEGRAM_ADMIN_CHANNEL=ваш_id_здесь
   ```

### Криптокошельки (для приема оплат):
В файле `.env` укажите свои кошельки:
```
USDT_TRC20_WALLET=ваш_TRC20_кошелек
USDT_ERC20_WALLET=ваш_ERC20_кошелек  
TON_WALLET=ваш_TON_кошелек
```

### Курсы обмена:
В файле `.env` укажите актуальные курсы:
```
RUB_TO_USDT_RATE=0.011
RUB_TO_TON_RATE=0.17
```

## 2. ЗАПУСК БЭКЕНДА

1. Откройте командную строку
2. Перейдите в директорию бэкенда:
   ```cmd
   cd C:\Users\sskut\Desktop\AIRBRO\backend
   ```
3. Убедитесь, что все зависимости установлены:
   ```cmd
   npm install
   ```
4. Запустите миграцию базы данных:
   ```cmd
   npx prisma migrate dev
   ```
5. Запустите сидинг данных:
   ```cmd
   npx tsx prisma/seed.ts
   ```
6. Запустите сервер:
   ```cmd
   npm run dev
   ```
7. Бэкенд должен запуститься на `http://localhost:3000`

## 3. СОЗДАНИЕ FRONTEND-КОНФИГУРАЦИИ

1. В директории `C:\Users\sskut\Desktop\AIRBRO\src\services\api.ts` создайте файл (если не существует) с настройками:
```ts
import axios from 'axios';

const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:3000/api';

const api = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Добавляем токен авторизации ко всем запросам
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default api;
```

## 4. ЗАПУСК ФРОНТЕНДА

1. Откройте новую командную строку
2. Перейдите в основную директорию:
   ```cmd
   cd C:\Users\sskut\Desktop\AIRBRO
   ```
3. Убедитесь, что зависимости установлены:
   ```cmd
   npm install
   ```
4. Запустите фронтенд:
   ```cmd
   npm run dev
   ```
5. Фронтенд будет доступен по `http://localhost:5173`

## 5. НАСТРОЙКА ИНТЕГРАЦИИ API

Для корректной работы формы обратной связи с Telegram в файле `C:\Users\sskut\Desktop\AIRBRO\src\services\api.ts` или в соответствующем компоненте формы добавьте:

```ts
// API для формы обратной связи
export const sendFeedback = async (formData) => {
  const response = await fetch('/api/telegram/send', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(formData),
  });
  return response.json();
};
```

## 6. ФУНКЦИОНАЛЬНОСТЬ, КОТОРАЯ БУДЕТ РАБОТАТЬ

### Регистрация и авторизация:
- `POST /api/auth/register` - Создание аккаунта
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение данных пользователя

### Продукты и каталог:
- `GET /api/products` - Просмотр всех продуктов
- `GET /api/products/:slug` - Просмотр конкретного продукта

### Корзина:
- `GET /api/cart` - Просмотр корзины
- `POST /api/cart/add` - Добавить в корзину
- `DELETE /api/cart/:itemId` - Удалить из корзины
- `POST /api/cart/clear` - Очистить корзину

### Покупки и оплаты:
- `POST /api/payments/crypto/create` - Создание криптоплатежа (генерация QR-кода)
- `GET /api/payments/:id/status` - Проверка статуса платежа
- `POST /api/payments/:id/confirm` - Подтверждение оплаты

### Личный кабинет:
- `GET /api/user/profile` - Просмотр профиля
- `PUT /api/user/profile` - Редактирование профиля
- `GET /api/user/subscriptions` - Просмотр подписок
- `GET /api/user/payments` - История платежей
- `POST /api/user/subscriptions/:id/cancel` - Отмена подписки

### Форма обратной связи:
- Интегрирована с Telegram через API
- Отправляет сообщения в указанный канал/чат

## 7. ТЕСТИРОВАНИЕ ПОЛНОГО ФЛОВА

После настройки выполните полный цикл для проверки:
1. Зарегистрируйтесь на сайте
2. Добавьте продукт в корзину
3. Перейдите к оплате (получите QR-код)
4. Подтвердите оплату через бэкенд
5. Проверьте создание подписки в личном кабинете
6. Проверьте уведомления в Telegram

Все функции будут работать, как описано в исходном плане! 🚀