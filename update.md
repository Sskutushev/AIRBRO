Хорошо, я понял задачу. Ты предоставил первоначальный план (v1) и подробный финальный аудит (v2). Моя задача — объединить их в единый, обновленный и приоритезированный "промпт-план" для реализации, используя v2 как основной источник правок.

Вот консолидированный план, который берет за основу твою первоначальную 6-уровневую структуру, но наполняет ее актуальными задачами и приоритетами из финального аудита (v2).

🎯 Финальный Промпт-План: Обновление AIRBRO 8.3/10 → 9.5/10
📋 ЗАДАЧА: Улучшить проект AIRBRO Business с текущей оценки 8.3/10 до 9.2-9.5/10, сфокусировавшись на критических и важных улучшениях, выявленных в ходе финального аудита.

1️⃣ ТЕСТИРОВАНИЕ (Текущая оценка: 7.0/10 → 10/10)
Что не так (Аудит v2):

Отсутствуют тесты для бэкенда (критично).

E2E тесты неактивны или отсутствуют.

Нет интеграционных тестов для форм (Auth, Payment).

Что делать (Приоритезировано):

Этап 1.1: 🔴 (Must Have) Тесты для Бэкенда

Задача: Написать тесты для всех API endpoints (controllers, services).

Где: backend/src/\*_/_.test.ts

Использовать: supertest для HTTP тестов, jest с моками Prisma.

Что покрыть (Приоритет):

/api/auth/\* - все сценарии (регистрация, вход, ошибки).

/api/cart/\* - CRUD операции.

/api/payments/\* - создание и проверка.

Этап 1.2: 🟡 (Should Have) E2E тесты

Задача: Включить, дописать и интегрировать Playwright тесты в CI/CD.

Где: tests/e2e/

Сценарии (Приоритет):

Регистрация → выбор тарифа → оплата.

Вход существующего пользователя.

Навигация по основным страницам.

Включить: Раскомментировать E2E job в .github/workflows/ci.yml.

Этап 1.3: 🟡 (Should Have) Интеграционные тесты для Фронтенда

Задача: Тесты для ключевых форм с моками API.

Что покрыть:

AuthPage: регистрация/вход, валидация.

AccountPage: отображение данных.

PaymentPage: процесс оплаты.

Использовать: MSW для мокирования API.

ЭтаP 1.4: (In Progress) Юнит-тесты для Компонентов

Статус: Отличная работа (ErrorBoundary, Button, OptimizedImage).

Задача: Поддерживать и расширять покрытие, особенно для новых компонентов.

Результат: Coverage 80%+, все тесты (Backend, Frontend, E2E) проходят в CI/CD.

2️⃣ БЕЗОПАСНОСТЬ (Текущая оценка: 7.5/10 → 10/10)
Что не так (Аудит v2):

Hardcoded секреты в environment.ts (JWT_SECRET).

Отсутствует Input Sanitization (риск XSS).

Отсутствует Helmet.js.

CSRF закомментирован.

Нет принудительного HTTPS.

Что делать:

Этап 2.1: 🔴 (Must Have) Убрать Hardcoded Secrets

Задача: Удалить секреты из environment.ts и docs/.

Решение:

TypeScript

// ❌ Было
// JWT_SECRET: z.string().default('d5f014ab7534ff7fe23c4f9d761bc640')

// ✅ Стало (убрать default, брать только из .env)
JWT_SECRET: z.string()
Добавить: Инструкцию по генерации секретов в docs/10_Security.md.

Этап 2.2: 🔴 (Must Have) Input Sanitization

Задача: Очистка всего пользовательского ввода на бэкенде.

Использовать: express-validator (для валидации) и xss или DOMPurify (для очистки).

Применить: Как middleware ко всем POST/PUT запросам, особенно body.

Этап 2.3: 🔴 (Must Have) Внедрить Helmet.js

Задача: Добавить базовые защитные заголовки.

Где: backend/src/server.ts

Использовать: app.use(helmet());

Этап 2.4: 🟡 (Should Have) HTTPS Redirect

Задача: Принудительный HTTPS в production.

Где: backend/src/middleware/httpsRedirect.ts

Применить: if (req.header('x-forwarded-proto') !== 'https') ... (только для NODE_ENV=production).

Этап 2.5: 🟡 (Should Have) CSRF

Задача: Раскомментировать и настроить csrf.ts (например, используя csurf).

Результат: Production-grade безопасность, 0 критичных уязвимостей.

3️⃣ КАЧЕСТВО КОДА И ПРОИЗВОДИТЕЛЬНОСТЬ (Оценка: 8.0/10 + 8.0/10 → 9.5/10)
Что не так (Аудит v2):

Кириллица в комментариях, console.log и ответах API (критично).

Хардкод данных (200+ строк) в ModulePopup.tsx.

Дублирование логики (проверка авторизации в Header).

Нет code splitting для крупных секций.

Что делать:

Этап 3.1: 🔴 (Must Have) Убрать кириллицу (Англификация)

Задача: Перевести ВЕСЬ код на английский язык.

Где:

Бэкенд: cartController.ts (Товар удален...) → Product removed...

Комментарии: // Компонент... → // Component for...

console.log и сообщения об ошибках.

Этап 3.2: 🟡 (Should Have) Рефакторинг ModulePopup.tsx

Задача: Вынести 200+ строк хардкода moduleData.

Решение:

Создать src/data/modules.ts или src/data/modules.json.

Использовать i18n для всех текстовых полей.

Импортировать данные в компонент, а не хранить их внутри.

Этап 3.3: 🟢 (Nice to Have) Code Splitting

Задача: Разделить крупные секции лендинга (кроме страниц).

Использовать: React.lazy() и Suspense для секций, которые не нужны на первом экране.

Этап 3.4: 🟢 (Nice to Have) Виртуализация списков

Задача: Если появятся длинные списки (например, история платежей), внедрить react-window или tanstack-virtual.

Этап 3.5: (Should Have) JSDoc и ESLint

Задача: Ужесточить .eslintrc.json (как в v1) и добавить JSDoc для всех services, hooks и utils.

Результат: Чистый, читаемый, интернационализированный код.

4️⃣ АРХИТЕКТУРА И STATE MANAGEMENT (Оценка: 8.5/10 + 8.5/10 → 9.5/10)
Что не так (Аудит v2):

SQLite для продакшна (критично для масштабирования).

Нет Repository layer (сервисы работают с Prisma напрямую).

Нет централизованной обработки ошибок на фронтенде (для async).

Что делать:

Этап 4.1: 🔴 (Must Have) Миграция на PostgreSQL

Задача: Заменить SQLite на PostgreSQL.

Где: backend/prisma/schema.prisma

Что сделать:

Изменить datasource db на postgresql.

Обновить DATABASE_URL в .env.example.

Создать миграцию: npx prisma migrate dev.

Обновить Railway config.

Этап 4.2: 🟡 (Should Have) Repository Pattern

Задача: Абстрагироваться от Prisma.

Где: backend/src/repositories/

Создать: UserRepository.ts, ProductRepository.ts, PaymentRepository.ts.

Зачем: Облегчить тестирование (моки репозиториев) и смену БД.

Этап 4.3: 🟡 (Should Have) Error Boundaries для Async

Задача: Централизованно ловить и обрабатывать ошибки API на фронте.

Решение:

Создать GlobalApiErrorBoundary.

Обернуть им App или использовать react-query (если он есть) с его onError хендлерами.

Показывать Toast уведомления (sonner) при ошибках.

Этап 4.4: (Should Have) Rate Limiting

Задача: Улучшить "базовый" rate limit (из v2).

Использовать: express-rate-limit с настройками из v1 (разные лимиты для /auth и /api).

Результат: Масштабируемая, production-ready архитектура.

5️⃣ ДОКУМЕНТАЦИЯ (Текущая оценка: 9.0/10 → 10/10)
Что не так (Аудит v2):

Дублирование (3 файла про API URL).

Нет curl примеров.

Что делать:

Этап 5.1: 🟡 (Should Have) Убрать дублирование

Задача: Удалить временные фиксы.

Удалить: API_URL_FIX.md, API_URL_FIX_FINAL.md, API_URL_REALITY_CHECK.md.

Этап 5.2: (Should Have) Добавить примеры кода

Задача: Дополнить 03_Backend_API.md живыми примерами.

Формат:

Bash

# Пример: Регистрация пользователя

curl -X POST http://localhost:3000/api/auth/register \
 -H "Content-Type: application/json" \
 -d '{"email":"test@test.com","password":"123456"}'
Этап 5.3: (Nice to Have) Переписать README и введение

Задача: Сделать README.md и 01_Introduction.md проще, "по-человечески", с акцентом на "Quick Start".

Результат: Документация, готовая для Open Source или новой команды.

6️⃣ UX/UI (Текущая оценка: 9.0/10 → 9.5/10)
Что не так (Аудит v2):

Проект уже в отличном состоянии (9.0/10).

Мелкие недочеты: хардкод размеров без media queries.

Что делать:

Этап 6.1: 🟢 (Nice to Have) Финальная полировка

Задача: Провести аудит компонентов на предмет хардкодных размеров (px вместо rem, отсутствие sm:, md:).

Проверить: Адаптивность всех форм и модальных окон на малых экранах.

Результат: Полированный, безупречный UI.

⏱️ ИТОГОВЫЙ ПЛАН РАБОТ (по Аудиту v2)
Этот план объединяет задачи по приоритету для максимальной эффективности.

Неделя 1: Критичные улучшения (Must Have)

Дни 1-2: Рефакторинг кода (Англификация). Убрать всю кириллицу из бэкенда и фронтенда.

День 3: Миграция на PostgreSQL. Настроить schema.prisma и Railway.

День 4: Security (Helmet, Sanitization, Secrets). Внедрить helmet, express-validator и убрать секреты из кода.

Дни 5-7: Backend тесты. Покрыть тестами auth, cart и payments.

Неделя 2: Важные улучшения (Should Have)

Дни 1-2: Repository Pattern. Создать слой репозиториев для Prisma.

День 3: Рефакторинг ModulePopup. Вынести хардкод в src/data.

Дни 4-6: E2E тесты. Настроить и запустить Playwright в CI/CD.

День 7: Error Handling (Frontend). Внедрить ErrorBoundary / Toast для API ошибок.

Неделя 3: Финальная полировка (Nice to Have)

День 1: Code Splitting и Документация. Разделить тяжелые секции, удалить \_FIX.md файлы.

День 2: Performance и UX Аудит. Проверить хардкод-размеры, оптимизировать.

День 3: Финальное регресс-тестирование.

🎖️ Финальный вердикт (v2):

Текущий проект: 8.3/10

После улучшений: 9.2-9.5/10

Ваш уровень: Strong Middle+ → Junior Senior
